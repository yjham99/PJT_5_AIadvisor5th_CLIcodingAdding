# 🤖 AI 어드바이저 자동화 시스템 가이드

이 시스템은 구글 Gemini AI를 활용하여 주식 시장 데이터를 심층 분석하고, 그 결과를 NotebookLM에 자동으로 저장하는 지능형 스케줄러입니다.

## 1. 개요
- **목적**: 키움 증권 데이터를 기반으로 각 분야 전문가(AI 참모)의 인사이트를 추출하고, 이를 지식 베이스(NotebookLM)에 체계적으로 축적합니다.
- **주요 기능**:
    1. **비동기 AI 분석**: 다수의 모델을 동시에 활용하여 빠른 분석 리포트 생성.
    2. **특징주 자동 발굴**: 시장의 급등주 및 뉴스 특징주를 AI가 실시간 분석.
    3. **주기적 자동 저장**: 스케줄러를 통한 지속적인 NotebookLM 업데이트.

## 2. 시스템 구성 모듈

### 2.1. AI 분석 엔진 (`model_scheduler.py`)
- **Gemini 2.0 Flash** 등 최신 모델을 활용한 비동기 분석 수행.
- 모델별 락(Lock) 기능을 통해 RPM(분당 호출 제한)을 안전하게 준수.

### 2.2. 데이터 분석기 (`data_analyzer.py`)
- CSV 데이터(잔고, 수급, 특징주)를 읽어 마크다운 리포트로 변환.
- **다중 인코딩 지원**: UTF-8, CP949 등 한국어 환경에 최적화.

### 2.3. 키움 증권 로그인 (`kiwoom_login.py`)
- **경로**: `C:\OpenAPI\opstarter.exe` 실행 지원.
- 윈도우 환경에서 안정적인 프로세스 시작 및 시뮬레이션 모드 지원.

### 2.4. 스케줄러 서비스 (`scheduler_service.py`)
- 전체 파이프라인을 특정 주기마다 반복 실행해주는 백그라운드 서비스.

## 3. 사용법 (Usage)

### 3.1. 환경 설정 (.env)
`.env` 파일에 아래 정보를 입력하세요.
```ini
GOOGLE_API_KEY=여러분의_API_키
```

### 3.2. 일회성 실행 (테스트)
```bash
python automation_orchestrator.py
```

### 3.3. 주기적 실행 (스케줄러 시작)
```bash
# 12시간마다 자동으로 분석 및 저장 수행
python scheduler_service.py --interval 12
```

## 4. AI 참모진 (Personas)
- **전략 최부장**: 거시 시장 전략 및 기관 의도 분석.
- **자금흐름 박차장**: 외인/기관 자금 이동 추적.
- **종목분석 이과장**: 보유 종목 진단 및 수익률 관리.
- **트레이딩 김대리**: **(New)** 특징주 발굴 및 단기 매매 전략.

---
**주의사항**:
- 특징주 분석을 위해서는 `특징주_*.csv` 형식의 파일이 업로드 폴더에 존재해야 합니다.
- 실제 운영 시에는 `C:\OpenAPI` 경로에 키움 OpenAPI가 설치되어 있어야 합니다.
